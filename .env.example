# Free/local mode — no API key required (uses Ollama)
# See docs/FREE_SETUP.md

# Optional — only if you switch generation.provider to openai in config/settings.yaml
# OPENAI_API_KEY=sk-your-key-here
